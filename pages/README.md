# Application Pages

The app deliberately uses one Streamlit entry point (`app.py`) and sidebar routing. This makes authentication and SQLite setup reliable on Streamlit Community Cloud while preserving the six requested screens: Home, Login, Dashboard, Report Lost Item, Browse Items, and Admin Panel.
